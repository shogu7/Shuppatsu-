const { createAnimeEmbeds } = require('./anime/animeEmbeds');
const { createMangaEmbeds } = require('./manga/mangaEmbeds');
const { createManwhaEmbeds } = require('./manwha/manwhaEmbeds');

module.exports = {
  createAnimeEmbeds,
  createMangaEmbeds,
  createManwhaEmbeds,
};
